---
layout: default
title: 会社名の自動整形ガイド
---
{% raw %}

# 🏢 会社名の自動整形ガイド

## 🎯 なぜ会社名も自動整形すべきか？

### ユーザーの声

```
「半角カナで入力してしまった...」
「全角英字で入力してしまった...」
「スペースが多すぎる...」
```

**解決策:**
```
✅ その場でリアルタイムに整形
✅ ユーザーは何も気にせず入力できる
✅ データベースに統一された形式で保存
```

---

## 📋 会社名整形の5つのルール

### ルール1: 半角カナ→全角カナ

**入力:** `ｶﾌﾞｼｷｶﾞｲｼｬｻﾝﾌﾟﾙ`  
**自動修正:** `カブシキガイシャサンプル`

```javascript
// 濁点・半濁点も正しく変換
'ｶﾞ' → 'ガ'
'ﾊﾟ' → 'パ'
'ｳﾞ' → 'ヴ'
```

**理由:**
- 半角カナは見た目が悪い
- データベースで検索しにくい
- 表記を統一すべき

---

### ルール2: 全角英数字→半角英数字

**入力:** `株式会社ＡＢＣ１２３`  
**自動修正:** `株式会社ABC123`

```javascript
// 英字・数字は半角に統一
'Ａ' → 'A'
'１' → '1'
```

**理由:**
- 会社名の英字部分は半角が標準
- データベースで検索しやすい
- ソート順が正しくなる

---

### ルール3: 前後の空白をトリム

**入力:** `　株式会社サンプル　`  
**自動修正:** `株式会社サンプル`

```javascript
// 全角・半角スペースを削除
str.trim()
```

**理由:**
- 意図しない空白を削除
- データの整合性を保つ

---

### ルール4: 連続する空白を1つに

**入力:** `株式会社　　サンプル商事`  
**自動修正:** `株式会社 サンプル商事`

```javascript
// 複数の空白を1つに
str.replace(/\s+/g, ' ')
```

**理由:**
- 見た目がきれい
- 検索がしやすい

---

### ルール5: 全角スペース→半角スペース

**入力:** `株式会社　サンプル`  
**自動修正:** `株式会社 サンプル`

```javascript
// 全角スペースを半角に統一
str.replace(/　/g, ' ')
```

**理由:**
- スペースは半角で統一
- データベースで扱いやすい

---

## 💻 実装コード

### InputFormatter.formatCompanyName()

```javascript
class InputFormatter {
    // 会社名整形
    static formatCompanyName(str) {
        let result = str;
        
        // 1. 半角カナ→全角カナ
        result = this.toFullWidthKana(result);
        
        // 2. 全角英数字→半角英数字
        result = this.toHalfWidthAlpha(this.toHalfWidthNumber(result));
        
        // 3. 前後の空白をトリム
        result = result.trim();
        
        // 4. 連続する空白を1つに
        result = result.replace(/\s+/g, ' ');
        
        // 5. 全角スペースを半角スペースに統一
        result = result.replace(/　/g, ' ');
        
        return result;
    }
    
    // 半角カナ→全角カナ
    static toFullWidthKana(str) {
        const kanaMap = {
            'ｶﾞ': 'ガ', 'ｷﾞ': 'ギ', 'ｸﾞ': 'グ', 'ｹﾞ': 'ゲ', 'ｺﾞ': 'ゴ',
            'ｻﾞ': 'ザ', 'ｼﾞ': 'ジ', 'ｽﾞ': 'ズ', 'ｾﾞ': 'ゼ', 'ｿﾞ': 'ゾ',
            'ﾀﾞ': 'ダ', 'ﾁﾞ': 'ヂ', 'ﾂﾞ': 'ヅ', 'ﾃﾞ': 'デ', 'ﾄﾞ': 'ド',
            'ﾊﾞ': 'バ', 'ﾋﾞ': 'ビ', 'ﾌﾞ': 'ブ', 'ﾍﾞ': 'ベ', 'ﾎﾞ': 'ボ',
            'ﾊﾟ': 'パ', 'ﾋﾟ': 'ピ', 'ﾌﾟ': 'プ', 'ﾍﾟ': 'ペ', 'ﾎﾟ': 'ポ',
            'ｳﾞ': 'ヴ',
            'ｱ': 'ア', 'ｲ': 'イ', 'ｳ': 'ウ', 'ｴ': 'エ', 'ｵ': 'オ',
            'ｶ': 'カ', 'ｷ': 'キ', 'ｸ': 'ク', 'ｹ': 'ケ', 'ｺ': 'コ',
            'ｻ': 'サ', 'ｼ': 'シ', 'ｽ': 'ス', 'ｾ': 'セ', 'ｿ': 'ソ',
            'ﾀ': 'タ', 'ﾁ': 'チ', 'ﾂ': 'ツ', 'ﾃ': 'テ', 'ﾄ': 'ト',
            'ﾅ': 'ナ', 'ﾆ': 'ニ', 'ﾇ': 'ヌ', 'ﾈ': 'ネ', 'ﾉ': 'ノ',
            'ﾊ': 'ハ', 'ﾋ': 'ヒ', 'ﾌ': 'フ', 'ﾍ': 'ヘ', 'ﾎ': 'ホ',
            'ﾏ': 'マ', 'ﾐ': 'ミ', 'ﾑ': 'ム', 'ﾒ': 'メ', 'ﾓ': 'モ',
            'ﾔ': 'ヤ', 'ﾕ': 'ユ', 'ﾖ': 'ヨ',
            'ﾗ': 'ラ', 'ﾘ': 'リ', 'ﾙ': 'ル', 'ﾚ': 'レ', 'ﾛ': 'ロ',
            'ﾜ': 'ワ', 'ｦ': 'ヲ', 'ﾝ': 'ン',
            'ｧ': 'ァ', 'ｨ': 'ィ', 'ｩ': 'ゥ', 'ｪ': 'ェ', 'ｫ': 'ォ',
            'ｯ': 'ッ', 'ｬ': 'ャ', 'ｭ': 'ュ', 'ｮ': 'ョ',
            '｡': '。', '｢': '「', '｣': '」', '､': '、', '･': '・',
            'ｰ': 'ー'
        };
        
        let result = str;
        // 濁点・半濁点付き文字を先に変換
        Object.keys(kanaMap).forEach(key => {
            if (key.length > 1) {
                result = result.split(key).join(kanaMap[key]);
            }
        });
        // 残りの文字を変換
        Object.keys(kanaMap).forEach(key => {
            if (key.length === 1) {
                result = result.split(key).join(kanaMap[key]);
            }
        });
        
        return result;
    }
}
```

---

## 🧪 テストケース

### テスト1: 半角カナの会社名

```javascript
const input = "ｶﾌﾞｼｷｶﾞｲｼｬｻﾝﾌﾟﾙｼｮｳｼﾞ";
const result = InputFormatter.formatCompanyName(input);

console.log(result);
// 期待: "カブシキガイシャサンプルショウジ"
```

### テスト2: 全角英数字が混在

```javascript
const input = "株式会社ＡＢＣ１２３";
const result = InputFormatter.formatCompanyName(input);

console.log(result);
// 期待: "株式会社ABC123"
```

### テスト3: 複数のスペース

```javascript
const input = "株式会社　　サンプル　　商事";
const result = InputFormatter.formatCompanyName(input);

console.log(result);
// 期待: "株式会社 サンプル 商事"
```

### テスト4: 前後の空白

```javascript
const input = "　株式会社サンプル　";
const result = InputFormatter.formatCompanyName(input);

console.log(result);
// 期待: "株式会社サンプル"
```

### テスト5: すべての問題が混在

```javascript
const input = "　ｶﾌﾞｼｷｶﾞｲｼｬ　ＡＢＣ　　１２３　";
const result = InputFormatter.formatCompanyName(input);

console.log(result);
// 期待: "カブシキガイシャ ABC 123"
```

---

## 📊 Before / After の比較

| 入力 | Before | After |
|---|---|---|
| `ｶﾌﾞｼｷｶﾞｲｼｬｻﾝﾌﾟﾙ` | そのまま保存 ❌ | `カブシキガイシャサンプル` ✅ |
| `株式会社ＡＢＣ` | そのまま保存 ❌ | `株式会社ABC` ✅ |
| `株式会社　　サンプル` | そのまま保存 ❌ | `株式会社 サンプル` ✅ |
| `　株式会社サンプル　` | そのまま保存 ❌ | `株式会社サンプル` ✅ |

---

## 🎯 リアルタイム整形のメリット

### メリット1: ユーザーが即座に確認できる

```
ユーザーが「ｶﾌﾞｼｷ」と入力
    ↓
即座に「カブシキ」に変換 ✅
    ↓
ユーザーは正しい形式を確認できる
```

**効果:**
- 入力ミスに気づきやすい
- 安心して入力できる
- 「これで合ってる？」の不安がゼロ

### メリット2: 送信時のエラーがゼロ

```
Before: 送信時に変換
    ↓
送信ボタンを押す
    ↓
「形式が正しくありません」エラー ❌
    ↓
修正して再送信

After: リアルタイム整形
    ↓
入力中に自動で正しい形式に ✅
    ↓
送信ボタンを押す
    ↓
成功！
```

**効果:**
- 送信エラーがゼロ
- ユーザーのストレス軽減
- 完了率が向上

### メリット3: データベースが統一される

```
Before: 送信時に変換（変換漏れあり）
    ↓
データベースに不統一なデータ ❌
- "株式会社ABC"
- "株式会社ＡＢＣ"
- "ｶﾌﾞｼｷｶﾞｲｼｬABC"

After: リアルタイム整形
    ↓
データベースに統一されたデータ ✅
- "株式会社ABC"
- "株式会社ABC"
- "カブシキガイシャABC"
```

**効果:**
- 検索がしやすい
- 重複チェックが正確
- データの品質が向上

---

## 💡 実装のポイント

### 1. IME対応は必須

```javascript
let isComposing = false;

input.addEventListener('compositionstart', () => {
    isComposing = true; // IME変換開始
});

input.addEventListener('compositionend', (e) => {
    isComposing = false; // IME変換終了
    handleInput(e); // 確定後に整形
});

input.addEventListener('input', (e) => {
    if (isComposing) return; // 変換中はスキップ
    handleInput(e);
});
```

### 2. カーソル位置を保持

```javascript
// 文字数の変化を計算
const diff = correctedValue.length - value.length;

// カーソル位置を調整
e.target.setSelectionRange(cursorPos + diff, cursorPos + diff);
```

### 3. 段階的に適用

```javascript
// ✅ 良い例: 1つずつ適用
result = toFullWidthKana(result);
result = toHalfWidthAlpha(result);
result = toHalfWidthNumber(result);
result = result.trim();

// ❌ 悪い例: 一度に複雑な処理
result = complexTransformation(result); // 何をやっているか分からない
```

---

## 🚀 応用例

### 応用1: 住所の整形

```javascript
static formatAddress(str) {
    let result = str;
    
    // 半角カナ→全角カナ
    result = this.toFullWidthKana(result);
    
    // 全角数字→半角数字（番地など）
    result = this.toHalfWidthNumber(result);
    
    // 前後の空白をトリム
    result = result.trim();
    
    return result;
}
```

### 応用2: 人名の整形

```javascript
static formatPersonName(str) {
    let result = str;
    
    // 半角カナ→全角カナ
    result = this.toFullWidthKana(result);
    
    // 前後の空白をトリム
    result = result.trim();
    
    // 姓名の間のスペースを1つに
    result = result.replace(/\s+/g, ' ');
    
    return result;
}
```

---

## 📚 まとめ

### なぜリアルタイム整形が重要か？

1. **ユーザーが即座に確認できる**
   - 入力中に正しい形式が分かる
   - 「これで合ってる？」の不安がゼロ

2. **送信時のエラーがゼロ**
   - 送信前に正しい形式になっている
   - ユーザーのストレス軽減

3. **データベースが統一される**
   - 検索がしやすい
   - データの品質が向上

### 会社名整形の5つのルール

1. ✅ 半角カナ→全角カナ
2. ✅ 全角英数字→半角英数字
3. ✅ 前後の空白をトリム
4. ✅ 連続する空白を1つに
5. ✅ 全角スペース→半角スペース

### 実装のポイント

1. ✅ IME対応は必須
2. ✅ カーソル位置を保持
3. ✅ 段階的に適用

---

**最終更新: 2025年12月25日**
**作成者: Brain-Friendly Code Lab**

{% endraw %}
