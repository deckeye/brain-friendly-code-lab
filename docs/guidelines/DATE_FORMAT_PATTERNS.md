---
layout: default
title: 日付フォーマット対応ガイド
---
{% raw %}

# 📅 日付フォーマット対応ガイド

## 🎯 業務アプリで対応すべき日付フォーマット一覧

### 1. 西暦フォーマット（基本）

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `2025-12-25` | ISO形式（ハイフン） | `2025-12-25` |
| `2025/12/25` | スラッシュ区切り | `2025-12-25` |
| `2025.12.25` | ドット区切り | `2025-12-25` |
| `2025 12 25` | スペース区切り | `2025-12-25` |
| `20251225` | 区切りなし | `2025-12-25` |

---

### 2. 全角文字混在

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `２０２５年１２月２５日` | 全角数字 | `2025-12-25` |
| `２０２５／１２／２５` | 全角数字＋スラッシュ | `2025-12-25` |
| `２０２５−１２−２５` | 全角ハイフン | `2025-12-25` |

---

### 3. 和暦フォーマット

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `令和6年12月25日` | 令和（漢字） | `2024-12-25` |
| `令和6-12-25` | 令和（ハイフン） | `2024-12-25` |
| `R6.12.25` | 令和（アルファベット） | `2024-12-25` |
| `R06/12/25` | 令和（ゼロ埋め） | `2024-12-25` |
| `平成31年4月30日` | 平成 | `2019-04-30` |
| `H31.4.30` | 平成（アルファベット） | `2019-04-30` |

---

### 4. 月日のみ（年省略）

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `12/25` | 今年の12月25日 | `2025-12-25` |
| `12-25` | 今年の12月25日 | `2025-12-25` |
| `1225` | 4桁（MMDD） | `2025-12-25` |

---

### 5. 年月のみ（日省略）

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `2025/12` | 年月のみ | `2025-12-01` |
| `2025-12` | 年月のみ | `2025-12-01` |
| `202512` | 6桁（YYYYMM） | `2025-12-01` |

---

### 6. 相対日付

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `今日` | 今日の日付 | `2025-12-25` |
| `明日` | 明日の日付 | `2025-12-26` |
| `昨日` | 昨日の日付 | `2025-12-24` |
| `+7` | 7日後 | `2026-01-01` |
| `-7` | 7日前 | `2025-12-18` |

---

### 7. 月の英語表記

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `Dec 25, 2025` | 英語（月-日-年） | `2025-12-25` |
| `25-Dec-2025` | 英語（日-月-年） | `2025-12-25` |
| `2025-Dec-25` | 英語（年-月-日） | `2025-12-25` |

---

### 8. 曜日付き

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `2025/12/25(木)` | 曜日付き | `2025-12-25` |
| `2025年12月25日(木)` | 曜日付き | `2025-12-25` |

---

### 9. エッジケース

| 入力例 | 説明 | 変換後 |
|---|---|---|
| `25/12/2025` | 日-月-年（欧州形式） | `2025-12-25` |
| `12/25/2025` | 月-日-年（米国形式） | `2025-12-25` |
| `2025年12月` | 年月のみ | `2025-12-01` |
| `12月25日` | 月日のみ | `2025-12-25` |

---

## 🧠 変換ロジックの優先順位

### 優先度1: 明確なフォーマット（100%の確率で正しい）

```javascript
// ISO形式
"2025-12-25" → "2025-12-25"

// 和暦
"令和6年12月25日" → "2024-12-25"

// 8桁数字（YYYYMMDD）
"20251225" → "2025-12-25"
```

---

### 優先度2: 区切り文字あり（90%の確率で正しい）

```javascript
// スラッシュ区切り（年が4桁）
"2025/12/25" → "2025-12-25"

// ドット区切り（年が4桁）
"2025.12.25" → "2025-12-25"
```

---

### 優先度3: 曖昧なフォーマット（要注意）

```javascript
// 月日のみ（年は今年と仮定）
"12/25" → "2025-12-25"

// 日月年 vs 月日年（地域によって異なる）
"12/25/2025" → "2025-12-25" (米国形式と仮定)
"25/12/2025" → "2025-12-25" (欧州形式と仮定)
```

**注意点:**
- `12/25/2025` は米国形式（月/日/年）
- `25/12/2025` は欧州形式（日/月/年）
- 数値の大小で判定（25 > 12なので、25は日）

---

## 💻 実装方針

### 方針1: 段階的パース

```javascript
function parseDate(input) {
    // 1. 全角→半角
    let normalized = toHalfWidth(input);
    
    // 2. 和暦→西暦
    if (isWareki(normalized)) {
        normalized = convertWarekiToSeireki(normalized);
    }
    
    // 3. パターンマッチング
    const patterns = [
        /^\d{4}-\d{2}-\d{2}$/,           // YYYY-MM-DD
        /^\d{4}\/\d{2}\/\d{2}$/,         // YYYY/MM/DD
        /^\d{4}\.\d{2}\.\d{2}$/,         // YYYY.MM.DD
        /^\d{8}$/,                       // YYYYMMDD
        // ... 他のパターン
    ];
    
    for (const pattern of patterns) {
        if (pattern.test(normalized)) {
            return parseWithPattern(normalized, pattern);
        }
    }
    
    // 4. フォールバック: JavaScriptのDate.parse()
    const date = new Date(normalized);
    if (!isNaN(date)) {
        return formatDate(date);
    }
    
    // 5. パース失敗
    return null;
}
```

---

### 方針2: ライブラリの活用

**推奨ライブラリ:**

1. **Day.js** (軽量・高機能)
   ```javascript
   import dayjs from 'dayjs';
   import customParseFormat from 'dayjs/plugin/customParseFormat';
   
   dayjs.extend(customParseFormat);
   
   const date = dayjs('2025/12/25', ['YYYY/MM/DD', 'YYYY-MM-DD', 'YYYYMMDD'], true);
   ```

2. **date-fns** (関数型・Tree-shakable)
   ```javascript
   import { parse, format } from 'date-fns';
   
   const date = parse('2025/12/25', 'yyyy/MM/dd', new Date());
   ```

3. **Luxon** (Intl API活用)
   ```javascript
   import { DateTime } from 'luxon';
   
   const date = DateTime.fromFormat('2025/12/25', 'yyyy/MM/dd');
   ```

---

## 🧪 テストケース

### テスト1: 西暦フォーマット

```javascript
parseDate("2025-12-25")   // → "2025-12-25" ✅
parseDate("2025/12/25")   // → "2025-12-25" ✅
parseDate("2025.12.25")   // → "2025-12-25" ✅
parseDate("20251225")     // → "2025-12-25" ✅
```

### テスト2: 和暦フォーマット

```javascript
parseDate("令和6年12月25日")  // → "2024-12-25" ✅
parseDate("R6.12.25")         // → "2024-12-25" ✅
parseDate("平成31年4月30日")  // → "2019-04-30" ✅
```

### テスト3: 全角文字

```javascript
parseDate("２０２５年１２月２５日")  // → "2025-12-25" ✅
parseDate("２０２５／１２／２５")    // → "2025-12-25" ✅
```

### テスト4: 月日のみ

```javascript
parseDate("12/25")    // → "2025-12-25" ✅ (今年と仮定)
parseDate("1225")     // → "2025-12-25" ✅
```

### テスト5: 無効な日付

```javascript
parseDate("2025-13-01")   // → null (13月は存在しない)
parseDate("2025-02-30")   // → null (2月30日は存在しない)
parseDate("abcdef")       // → null
```

---

## 📚 まとめ

### 対応すべき主要フォーマット

1. ✅ 西暦（YYYY-MM-DD、YYYY/MM/DD等）
2. ✅ 和暦（令和6年12月25日、R6.12.25等）
3. ✅ 全角文字混在
4. ✅ 月日のみ（年省略）
5. ✅ 年月のみ（日省略）
6. ✅ 相対日付（今日、明日等）
7. ✅ 英語月名（Dec 25, 2025等）
8. ✅ 曜日付き

### 実装のポイント

1. ✅ 全角→半角変換を最初に実施
2. ✅ 和暦→西暦変換を優先
3. ✅ パターンマッチングで段階的にパース
4. ✅ バリデーションを忘れずに
5. ✅ ライブラリの活用を検討

---

**最終更新: 2025年12月25日**  
**作成者: Brain-Friendly Code Lab**

{% endraw %}

